import 'package:flutter/material.dart';

class JewelsScreen extends StatelessWidget {
  const JewelsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Jewels')),
      body: const Center(child: Text('Jewels — coming soon')),
    );
  }
}
