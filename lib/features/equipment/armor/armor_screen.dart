import 'package:flutter/material.dart';

class ArmorScreen extends StatelessWidget {
  const ArmorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Armor')),
      body: const Center(child: Text('Armor — coming soon')),
    );
  }
}
