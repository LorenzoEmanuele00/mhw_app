import 'package:flutter/material.dart';

class TalismansScreen extends StatelessWidget {
  const TalismansScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Talismans')),
      body: const Center(child: Text('Talismans — coming soon')),
    );
  }
}
